第一部份：點解 Django 可以一次過顯示晒唔同類別嘅商品？

以前做靜態網頁，你可能要分開起好幾個 HTML 檔案（例如 apps.html、devices.html、accessories.html），然後人手將商品一張張「寫死」入去。

但 Django 同資料庫（PostgreSQL / pgAdmin 4）嘅運作方式完全唔同：
1. 同一個大倉庫（Single Table）： 喺資料庫入面，你嘅 Apps、Devices 同 Accessories 通常唔係四散各地，而係統一儲存喺**同一張表（例如叫 Product 表）**入面。
每一件商品只係多咗一個欄位叫 genre（類別），用嚟標記佢到底係 'app'、'device' 定 'accessory'。
2. 「一網打盡」嘅指令： 當你喺後端執行 Product.objects.filter(...) 或者 Product.objects.all() 嗰陣，你其實係同 Django 講：「去倉庫幫我將所有符合條件嘅橫排數據（Rows）全部搬出嚟！」不論佢嘅類別欄位寫住咩，總之只要符合關鍵字，就全部打包。
3. 統一的清單（JSON 格式）： Django 搬完貨之後，會將所有唔同類別嘅商品，排好隊變成一串長長嘅清單塞畀前端 JavaScript。喺前端眼入面，佢只會睇到一堆長得一模一樣嘅資料物件：
